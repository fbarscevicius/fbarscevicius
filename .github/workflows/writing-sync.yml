name: writing sync

# Pulls the latest essays from the anvilwright RSS feed into the README,
# between the BLOG-POST-LIST markers. Runs daily and on demand.

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/blog-post-workflow@v1
        with:
          feed_list: "https://anvilwright.com/rss.xml"
          max_post_count: 5
          # dateformat tokens: lowercase mm is month, capital MM is minutes.
          date_format: "yyyy-mm-dd"
          # Lead each item with a newline so the first item is not glued onto the
          # same line as the START comment (which would render as raw text).
          template: "$newline- [$title]($url) <sub>$date</sub>"
          commit_message: "sync writing list from feed"
